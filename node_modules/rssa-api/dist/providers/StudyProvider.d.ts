import React, { ReactNode } from 'react';
import RssaClient from './RssaClient';
interface StudyContextType {
    studyApi: RssaClient;
}
interface StudyProviderProps {
    config: {
        api_url_base: string;
        study_id: string;
    };
    children: ReactNode;
}
declare const StudyProvider: React.FC<StudyProviderProps>;
declare const useStudy: () => StudyContextType;
export { StudyProvider, useStudy };
